node_modules
.next
.vercel

# Env: tutup semua, kecuali contoh yang memang dibagikan.
# Pola lama (.env, .env.local, .env.neon, .env.*.local) meloloskan
# .env.production — dan Next.js MEMUAT file itu saat `next build`, jadi
# isinya ikut ke bundle produksi.
.env*
!.env.example
!.env.demo.example

# Build artifact
*.tsbuildinfo
*.log

.DS_Store
next-env.d.ts
/prisma/dev.db
public/uploads
private-uploads
